#include "utils.h"
#include "timer.h"

__global__
void blelloch_scan_kernel(ull * const d_arr, ull * const d_out, int N)
{
  return;
}

__global__
void shared_blelloch_scan_kernel(ull * const d_arr, ull * const d_out, int N)
{
  return;
}

float blelloch_scan(const ull * const h_arr, ull * const h_out, int N)
{
  return 0;
}

float shared_blelloch_scan(const ull * const h_arr, ull * const h_out, int N)
{
  return 0;
}

